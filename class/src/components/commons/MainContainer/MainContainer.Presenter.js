import { MainContainer__Container } from "./MainContainer.Styles";

export default function MainContainerUI({children}){

    return(
        <MainContainer__Container>
            {children}
        </MainContainer__Container>
    )
}